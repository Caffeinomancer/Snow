using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;
/*
[CustomEditor(typeof(ThreeDScreenAdjuster))]
public class ThreeDScreenAdjusterEditor : Editor
{
    /*public override void OnInspectorGUI()
    {
        DrawDefaultInspector();

        ThreeDScreenAdjuster myScript = (ThreeDScreenAdjuster)target;
        if(GUILayout.Button("Generate Screen"))
        {
            myScript.GenerateScreen();
        }

        if (GUILayout.Button("Delete Screen"))
        {
            myScript.DeleteScreen();
        }
    }
}*/
